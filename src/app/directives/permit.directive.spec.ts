import { PermitDirective } from './permit.directive';

describe('PermitDirective', () => {
  it('should create an instance', () => {
    const directive = new PermitDirective();
    expect(directive).toBeTruthy();
  });
});
